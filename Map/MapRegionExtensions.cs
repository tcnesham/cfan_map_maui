using GoogleApi.Entities.Common;

namespace CFAN.SchoolMap.Maui.GoogleMaps.Extensions
{
    public static class MapRegionExtensions
    {
        /*
        public static ViewPort ToViewPort(this MapRegion b)
        {
            var vp = new ViewPort();
            vp.NorthEast = new Location(b.FarRight.Latitude, b.FarRight.Longitude);
            vp.SouthWest = new Location(b.NearLeft.Latitude, b.NearLeft.Longitude);
            return vp;
        }
        */
    }
}